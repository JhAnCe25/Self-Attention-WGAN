name: gans310
channels:
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - --index-url https://download.pytorch.org/whl/cu128
      - torch
      - torchvision
      - --extra-index-url https://pypi.org/simple
      - numpy
      - scipy
      - pillow
      - joblib
      - datasets
      - matplotlib
      - tqdm
      - pyyaml
      - pytest
